"rescript-surrealdb": major
---

Tighten the public binding surface around explicit output domains, `.json()` state,
`ApiPromise` mode tracking, `RangeBound` constructor inputs, and `RecordId`
compound id fidelity. Add packed-consumer proof to the release gate.
