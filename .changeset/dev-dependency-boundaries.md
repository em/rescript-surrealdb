---
"rescript-surrealdb": patch
---

Keep `rescript-vitest` and `rescript-nodejs` in development-only package metadata so consumers do not install the binding's internal test dependencies at runtime.
